<!DOCTYPE html>
<html lang="en">
	<head>
		<title>Do-U-Know</title>
		<%= csrf_meta_tags %>
		<%= render partial: "layouts/google_analytics" %>
		<%= render partial: "layouts/share_this"%>
		<%#= render partial: "layouts/one_signal_notification"%>
		<%= stylesheet_link_tag 'application', media: 'all' %>
		<%= javascript_include_tag 'application' %>
		<link href='//fonts.googleapis.com/css?family=Open+Sans:700,700italic,800,300,300italic,400italic,400,600,600italic' rel='stylesheet' type='text/css'>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
		<meta name="author" content="">
		<meta name="keywords" content="do you know, unknown facts, did you know, facts, science fact, health fact, Sex fact, mythology fact, historical facts" />
		<%= yield :meta_og %>
    <script type="text/javascript">
       jQuery(document).ready(function($) {
       	$(".scroll").click(function(event){		
       		event.preventDefault();
       		$('html,body').animate({scrollTop:$(this.hash).offset().top},900);
       	});
       });
    </script>
	</head>

  <body>
  	<%= render partial: "layouts/nav_bar" %>
		<div class="full">
		  <div class="col-md-9 main">
	      <div class="banner-section">
	    		<%= yield %>
	    	</div>
		    <%= render partial: "layouts/right_section" %>
		    <div class="clearfix"> </div>
		  </div>
		  <div class="clearfix"> </div>
		</div>
		<script type="text/javascript">
		   $(document).ready(function() {
		    /*
		    var defaults = {
		          containerID: 'toTop', // fading element id
		      containerHoverID: 'toTopHover', // fading element hover id
		      scrollSpeed: 1200,
		      easingType: 'linear' 
		      };
		    */
		    
		    $().UItoTop({ easingType: 'easeOutQuart' });
		    
		   });
		</script>
		<a href="#home" id="toTop" class="scroll" style="display: block;"> <span id="toTopHover" style="opacity: 1;"> </span></a>
	</body>    	
</html>
